const { PrismaClient } = require('@prisma/client');\r\n\r\nconst prisma = new PrismaClient();\r\n\r\nasync function main() {\r\n  console.log('Apagando todos os produtos do banco de dados...');\r\n  const result = await prisma.product.deleteMany({});\r\n  console.log(`Sucesso: ${result.count} produtos apagados.`);\r\n}\r\n\r\nmain()\r\n  .then(async () => {\r\n    await prisma.$disconnect();\r\n  })\r\n  .catch(async (e) => {\r\n    console.error(e);\r\n    await prisma.$disconnect();\r\n    process.exit(1);\r\n  });
